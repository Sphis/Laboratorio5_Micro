    unsigned int time = 30;
	unsigned int time_2 = 5;
	for (int i = 0; i < time; i++){
		mostrar_num(9,0);
		delay(tiempo);
		mostrar_num(9,1);
		delay(tiempo);
				
	}
	for (int i = 0; i < time_2; i++){
		mostrar_num(0,0);
		delay(tiempo);
		mostrar_num(0,1);
		delay(tiempo);
				
	}
